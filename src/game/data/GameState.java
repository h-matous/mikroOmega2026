package game.data;

/**
 * The GameState Enum represents each state the Game can currently be in, stored in GameData
 */
public enum GameState {
    TITLE_MAIN,
    TITLE_STATISTICS,
    TITLE_SETTINGS,
    GAME_MAIN,
    GAME_PAUSE,
    GAME_LOST,
    EXIT
}